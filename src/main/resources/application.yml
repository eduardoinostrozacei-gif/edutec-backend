spring:
  datasource:
    url: jdbc:mysql://hopper.proxy.rlwy.net:45811/edutec_db?sslMode=REQUIRED&allowPublicKeyRetrieval=true
    username: root
    password: QgUqLOreaEnxEmWasUIbvwwbZhMEtOMy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none           # Flyway gestiona el esquema
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 1

server:
  port: 8080

jwt:
  secret: cambia_esta_clave_ultra_secreta
  expiration: 86400000

  server:
    error:
      include-message: always
      include-binding-errors: always

  logging:
    level:
      org.springframework.web: DEBUG
      org.hibernate.SQL: INFO

      management:
        endpoints:
          web:
            exposure:
              include: mappings


